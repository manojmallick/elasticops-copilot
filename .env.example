# Elasticsearch Configuration
# Mode: 'cloud' (uses Cloud ID) or 'local' (uses URL)
ELASTIC_MODE=cloud
ELASTIC_CLOUD_ID=your_cloud_id_here
ELASTIC_API_KEY=your_api_key_here
ELASTIC_URL=http://localhost:9200

# LLM Configuration (Future Enhancement - Not Currently Implemented)
# GEMINI_API_KEY=your_gemini_api_key_here
# GEMINI_MODEL=gemini-1.5-flash

# Application Configuration
EMBED_DIMS=384
APP_URL=http://localhost:3000

# Public URL for client-side citation links
# In production, set this to your Vercel domain (e.g., https://elasticops-copilot.vercel.app)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Webhook Security (for Agent Builder integration)
# Set this to a random string to secure your webhook endpoint
# ELASTICOPS_WEBHOOK_SECRET=your_random_secret_here
